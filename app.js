var rows = prompt("How many rows for your mutliplication table");
var cols = prompt("How many rows for your mutliplication table");
if ( row == "" || rows == null )
    rows = 10;
if ( cols == "" || cols == null )
    cols = 10;

